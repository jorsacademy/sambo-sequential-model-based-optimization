# SAMBO Sequential Model-Based Optimization

Example project showing sequential model-based optimization (SMBO) with the `sambo` Python package on a stochastic inventory-control policy.

> Implementation is developed on a feature branch and proposed via pull request.
